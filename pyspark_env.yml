name: pyspark_env
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python ~=3.12.10
  - jupyter ~=1.1.1
  - pyspark ~=3.5.5
  - openjdk ~=17.0.14
variables:
  JAVA_HOME: $CONDA_PREFIX/lib/jvm
